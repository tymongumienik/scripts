[project]
name = "pkd-label"
version = "0.1.0"
description = "Script for PKD (Polska Klasyfikacja Działalności) code data fetching via scraping"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
]
